import 'dotenv/config'; import { pool } from './src/db/pool'; pool.query("SELECT column_name, data_type, character_maximum_length FROM information_schema.columns WHERE table_name = 'projek_planning_log'").then(res => console.table(res.rows)).catch(console.error).finally(() => process.exit());
