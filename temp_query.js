const { Client } = require('pg'); async function run() { const client = new Client({ connectionString: 'postgresql://postgres:postgres@localhost:5432/sparta' }); await client.connect(); const res = await client.query('SELECT status, kategori_pekerjaan FROM pengawasan WHERE id_gantt IN (SELECT id_gantt FROM pengawasan_gantt WHERE id_toko = 2498 AND tanggal_pengawasan = \'' + '2026-07-24' + '\')'); console.log(res.rows); process.exit(); } run();
