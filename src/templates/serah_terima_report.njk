<!doctype html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <style>
    @page {
      size: A4;
      margin: 18mm 14mm;
    }

    body {
      font-family: Arial, sans-serif;
      font-size: 11px;
      color: #1f2937;
      margin: 0;
      position: relative;
    }

    .watermark {
      position: fixed;
      top: 50%;
      left: 50%;
      width: 240px;
      transform: translate(-50%, -50%);
      opacity: 0.08;
      z-index: 0;
      pointer-events: none;
    }

    .content {
      position: relative;
      z-index: 1;
    }

    .header {
      text-align: center;
      margin-bottom: 16px;
      border-bottom: 3px double #e30613;
      padding-bottom: 10px;
    }

    .title {
      margin: 0;
      font-size: 22px;
      font-weight: 700;
      letter-spacing: 0.5px;
      text-transform: uppercase;
      color: #e30613;
    }

    .subtitle {
      margin: 4px 0 0;
      color: #4b5563;
      font-size: 12px;
    }

    .meta {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 14px;
    }

    .meta td {
      border: 1px solid #c7cbd1;
      padding: 6px 8px;
      vertical-align: top;
    }

    .meta .label {
      width: 170px;
      font-weight: 600;
      background: #f6f8fb;
    }

    .section-title {
      font-size: 13px;
      font-weight: 700;
      margin: 14px 0 6px;
      color: #e30613;
      text-transform: uppercase;
      border-bottom: 1px solid #e30613;
      padding-bottom: 4px;
    }

    .category-header {
      font-size: 12px;
      font-weight: 700;
      margin-top: 12px;
      padding: 6px 0 4px;
      border-bottom: 1px solid #e30613;
      text-transform: uppercase;
      color: #b91c1c;
    }

    .items {
      width: 100%;
      border-collapse: collapse;
      margin-top: 8px;
    }

    .items th,
    .items td {
      border: 1px solid #aeb4bc;
      padding: 5px 6px;
      vertical-align: top;
      font-size: 10px;
    }

    .items th {
      background: #e30613;
      color: #ffffff;
      font-weight: 600;
      text-transform: uppercase;
    }

    .num {
      text-align: right;
      white-space: nowrap;
    }

    .center {
      text-align: center;
    }

    .summary {
      margin-top: 12px;
      width: 100%;
      border-collapse: collapse;
    }

    .summary td {
      border: 1px solid #c7cbd1;
      padding: 6px 8px;
    }

    .summary .label {
      width: 220px;
      background: #f6f8fb;
      font-weight: 600;
    }

    .footer {
      margin-top: 16px;
      font-size: 10px;
      color: #6b7280;
      text-align: right;
    }
  </style>
</head>
<body>
  {% if watermark_logo_path %}
  <img src="{{ watermark_logo_path }}" class="watermark" alt="watermark" />
  {% endif %}

  <div class="content">
  <div class="header">
    <h1 class="title">Berita Acara Serah Terima</h1>
  </div>

  <table class="meta">
    <tr>
      <td class="label">Nomor ULOK</td>
      <td>{{ toko.nomor_ulok or '-' }}</td>
      <td class="label">Tanggal Serah Terima</td>
      <td>{{ created_at_formatted }}</td>
    </tr>
    <tr>
      <td class="label">Nama Toko</td>
      <td>{{ toko.nama_toko or '-' }}</td>
      <td class="label">Kode Toko</td>
      <td>{{ toko.kode_toko or '-' }}</td>
    </tr>
    <tr>
      <td class="label">Proyek</td>
      <td>{{ toko.proyek or '-' }}</td>
      <td class="label">Cabang</td>
      <td>{{ toko.cabang or '-' }}</td>
    </tr>
    <tr>
      <td class="label">Alamat</td>
      <td colspan="3">{{ toko.alamat or '-' }}</td>
    </tr>
    <tr>
      <td class="label">Nama Kontraktor</td>
      <td>{{ toko.nama_kontraktor or '-' }}</td>
      <td class="label">Lingkup Pekerjaan</td>
      <td>{{ toko.lingkup_pekerjaan or '-' }}</td>
    </tr>
  </table>

  <div class="section-title">Daftar Pekerjaan</div>

  {% for group in grouped_items_list %}
  <div class="category-header">{{ loop.index }}. {{ group.category }}</div>
  <table class="items">
    <thead>
      <tr>
        <th style="width:28px">No</th>
        <th>Jenis Pekerjaan</th>
        <th class="center" style="width:90px">Desain</th>
        <th class="center" style="width:90px">Kualitas</th>
        <th class="center" style="width:90px">Spesifikasi</th>
        <th class="center" style="width:90px">Dokumentasi</th>
      </tr>
    </thead>
    <tbody>
      {% for item in group.items %}
      <tr>
        <td class="center">{{ loop.index }}</td>
        <td>{{ item.jenis_pekerjaan or '-' }}</td>
        <td class="center">{{ item.desain or '-' }}</td>
        <td class="center">{{ item.kualitas or '-' }}</td>
        <td class="center">{{ item.spesifikasi or '-' }}</td>
        <td class="center">
          {% if item.foto %}
          <a href="{{ item.foto }}" target="_blank" rel="noopener">Lihat Foto</a>
          {% else %}
          -
          {% endif %}
        </td>
      </tr>
      {% endfor %}
    </tbody>
  </table>
  {% endfor %}
  {% if grouped_items_list.length === 0 %}
  <table class="items">
    <tr>
      <td colspan="6" class="center">Belum ada item opname.</td>
    </tr>
  </table>
  {% endif %}

  <div class="footer">
    Digenerate otomatis oleh sistem SPARTA pada {{ generated_at }}
  </div>
  </div>
</body>
</html>
